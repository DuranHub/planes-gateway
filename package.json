{
  "name": "graphql-gateway",
  "main": "index.js",
  "scripts": {
    "start": "mesh start --require dotenv/config",
    "dev": "mesh dev --require dotenv/config",
    "build": "mesh build"
  },
  "dependencies": {
    "@graphql-mesh/cli": "^0.88.5",
    "@graphql-mesh/openapi": "^0.97.5",
    "graphql": "^16.8.1"
  }
}